version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "5001:5000"
    environment:
      - PORT=5000
    restart: always

  frontend:
    build: ./frontend
    ports:
      - "3001:3000"
    stdin_open: true
    tty: true
    depends_on:
      - backend
    restart: always
